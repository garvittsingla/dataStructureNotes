# Stacks and Queues


Stacks can be used to imropve performance if there are 2 loops and thr second loop is dependent on first loop in any way like, there is a chance we can use stack to improve its performance

If there is a sequence in which we want something to happen, then we can use the stack

## Stack
Stack is a ds with LIFO principle, thing follows last in first out mechanism as simple as that.

Implementation 