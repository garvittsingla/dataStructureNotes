## Sliding window
Sliding window is the best way to maintain a window in a string, array and based on that , compute the answer then shrink the window or maintain it as it is.

1. Constant window -> The size of window is constant.
2. Variable -> The size of window is dynamic and changes according to the conditions


> Most of the questions of sliding window can be done with the help of 2 loops with n2 complexity
